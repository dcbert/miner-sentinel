# Test-only dependencies for Django backend test suite
# Install: pip install -r requirements.txt -r requirements-test.txt
# Or in Docker dev: extend the backend image or use pip install -r requirements-test.txt after base

pytest>=8.0
pytest-django>=4.8
pytest-cov>=4.1
factory-boy>=3.3
responses>=0.25
faker>=22.0
coverage>=7.4